#include "tests.h"

void run_tests() {
	// PQueue<int> h;
	// h.push(5);
	// assert(h.size() == 1);
	// h.push(7);
	// assert(h.size() == 2);
	// h.push(-1);
	// assert(h.size() == 3);
	// assert(h.top() == -1);
	// assert(h.empty() == false);
	// h.pop();
	// assert(h.top() == 5);
	// assert(h.size() == 2);
	// h.pop();
	// assert(h.top() == 7);
	// h.pop();
	// assert(h.size() == 0);
	// assert(h.empty() == true);
}